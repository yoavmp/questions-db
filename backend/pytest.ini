[pytest]
testpaths = tests
# Contract/category/DTO/adapter tests only. They use a temporary sqlite DB and
# never touch backend/src/database/app.db. The generator-adapter full-assembly
# tests self-skip unless the exam_generator submodule and its deps are present.
